extends layout.pug

block content
  div(ng-controller="indexController")
    div#terminal
    input(type="text" ng-model="command" enter="sendCommand()")#input
    //button(ng-click="sendCommand()") Send
